# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **This file is shared memory.** It captures the project's current *posture* and *why* decisions were made — not just what the code is. Humans and LLMs both read it. When you make a non-obvious choice, add a dated entry to the **Decision Log** with the reasoning. See **Maintaining this file** at the bottom.

## What this is

`frontdoorapp-web` is the web client for **frontdoorapp.co** — a **managed, done-for-you** digital-presence platform for businesses: it sets up a business's entire online presence (website, social media, business email, reviews, content) and keeps it running on autopilot. Right now the repo contains the **marketing landing page**; the product itself is in early development. Product and brand narrative live in [README.md](README.md); this file owns engineering posture and decisions.

**Posture (2026-06):** a static, single-page Next.js site — **live in production at [frontdoorapp.co](https://frontdoorapp.co)** on Vercel (zero-config; auto-deploys on every push to `main`). No backend, database, auth, or API routes yet.

## Commands

```bash
npm install          # install deps (Node 18.18+, Node 20+ recommended)
npm run dev          # dev server → http://localhost:3000
npm run build        # production build — ALSO runs the TypeScript type-check
npm start            # serve the production build (run after build)
npm test             # run the Vitest suite once (jsdom + React Testing Library)
npm run test:watch   # Vitest in watch mode
```

- **Quality gates: `npm test` (Vitest) + `npm run build` (type-check).** There is no ESLint config. Tests live in `tests/` and cover the interactive/logic surface (open/closed hours, email capture, booking form, menu modal + order, the waitlist route); presentational sections are smoke-checked (8 services render, copy, no em-dashes). Run both before declaring a change done.
- **Test tooling is excluded from `next build`'s type-check** (`tsconfig.json` excludes `vitest.config.ts`, `vitest.setup.tsx`, `tests/`): a vite-vs-vitest type-version clash otherwise fails the app build. Tests are validated by *running* them, not by the Next type-checker. Vitest transforms JSX with the automatic runtime (`esbuild.jsx: "automatic"` in `vitest.config.ts`) and mocks `next/image` → plain `<img>` in the setup file.
- **Deploy:** linked to Vercel and **live at frontdoorapp.co** — every push to `main` auto-deploys to production (PRs get preview URLs). Manual deploy: `vercel --prod` via the Vercel CLI.

## Architecture (big picture)

Next.js 15 **App Router** + TypeScript + **Tailwind CSS v4**. Every route is statically prerendered.

- `app/layout.tsx` — root layout. Owns fonts (`next/font`), all SEO / OpenGraph / Twitter metadata, and the `viewport` theme color. Fonts are exposed as CSS variables (`--font-inter`, `--font-jakarta`) and consumed by the theme.
- `app/page.tsx` — composes the page from section components, in order: Header → Hero → Problem → HowItWorks → Features (the 8 included services) → Mission → CTA → Footer.
- `app/demo/page.tsx` — a **mock bakery website** (Bella's Bakery): photo-cover hero, its own site nav, about/hours/contact, a photo menu, gallery, a `BookingForm` (the only client piece here), reviews, and footer. Photos are royalty-free JPEGs in `public/demo/`, served via plain `<img>` (not `next/image`, to skip optimization/sharp setup for decorative mock content). It's the clickable "live example" linked from the hero PresenceCard; `noindex`.
- `app/globals.css` — **the design system lives here, not in a JS config** (Tailwind v4 is CSS-first). The `@theme` block defines brand tokens (`--color-ink`, `--color-brand`, `--font-display`, animation tokens…), which auto-generate utilities like `bg-brand`, `text-ink`, `font-display`, `animate-float`. Base element styles, keyframes, and the `prefers-reduced-motion` guard are here too. **Dark mode also lives here:** a `.dark` block flips the single-purpose color *variables* (so `bg-cloud`/`text-ink-soft`/`border-line` adapt automatically) plus explicit rules for `bg-white`/brand text; `.force-light` pins `/demo` to light. The marquee (`--animate-marquee*`) and scroll-`reveal` effects are here too.
- `components/ThemeToggle.tsx` — client toggle that flips `.dark` on `<html>` and persists to `localStorage`. The initial theme (localStorage → system preference) is applied before paint by a small inline script in `app/layout.tsx`.
- `components/` — one section per file (PascalCase filenames, named exports). Logo: the brand mark is the real asset `public/logo-lockup.png` (door + “frontdoorapp.co” lockup), used in the header and the PresenceCard “powered by” via `next/image`; `Wordmark` is the **frontdoorapp.co** text wordmark (dark footer); `DoorMark` is the hand-built vector door, now decorative only (Mission band + the `app/icon.svg` favicon). Plus `icons` and the lone client component `EmailCapture`.

**Server-first:** every component is a React Server Component except `EmailCapture` (`'use client'`), the only interactive piece. Add `'use client'` only when a component genuinely needs state / effects / event handlers.

**Brand:** navy `--color-ink` (`#0f1f47`) + blue `--color-brand` (`#2563eb`); Plus Jakarta Sans for display, Inter for body. Use the named tokens (`text-ink`, `bg-brand`, `font-display`) in components — don't hardcode hex. **The brand name in copy is `frontdoorapp.co`** (lowercase, with “.co” in brand blue) — never “Frontdoor”. Copy also says “businesses” (not “small business”) and “social media” (not “social”).

### Non-obvious things that will bite you

- **Tailwind v4 renamed gradient utilities:** use `bg-linear-to-*` / `bg-radial`, **not** v3's `bg-gradient-to-*`. The old names silently emit no CSS (no build error) — the gradient just won't render.
- **Two copies of the logos, on purpose:** `assets/` is referenced by README.md so it renders on GitHub; `public/` is served by the app and used as the OpenGraph/social image. Change a logo → update both.
- **Favicon / app icons use Next file conventions, not metadata:** `app/icon.svg` (a hand-built door mark — *not* the real brand asset; see Known gaps) is the favicon; `app/apple-icon.jpg` is the iOS touch icon. Next wires the `<link>` tags automatically — there are intentionally none in `metadata.icons`.
- **Copy uses typographic punctuation** (’ “ ” —) on purpose: it renders better and sidesteps React's unescaped-entities lint.
- `next-env.d.ts` is generated by Next and gitignored — don't edit or commit it.

## Decision Log

Newest on top. Each entry = the decision + the *why* (and the option rejected, if any). Don't rewrite history; if a decision is reversed, add a new entry noting it.

- **2026-07-02 — Added a test suite: Vitest + React Testing Library** (over Jest or Playwright). Vitest for speed and zero-config ESM/TS with the existing Vite-style tooling; RTL + jsdom for the client components; no E2E runner yet (the site is static, so unit + component + route tests give the most coverage per setup cost). Scope = the pieces with real logic/behavior: `OpenStatus` open/closed hours (pure `status()`/`fmt()`, now exported for testing), `EmailCapture` (validation + submit + honeypot), `BookingForm` (slots/defaults/confirm), `Menu` (modal + demo order), and `/api/waitlist` (BotID/honeypot/validation/store, with `botid/server` + `@upstash/redis` mocked); plus a `Features` smoke test. **Gotchas:** (1) `next build` type-checks the whole tree and chokes on a vite/vitest type-version mismatch in `vitest.config.ts` → excluded those files in `tsconfig.json`; (2) JSX needed `esbuild.jsx: "automatic"` in the Vitest config or every render threw "React is not defined". `npm test` is now a first-class gate alongside `npm run build`.
- **2026-07-02 — Demo open/closed status is computed from UTC store hours.** `components/demo/OpenStatus.tsx` (client) compares the visitor's clock (`new Date()`, in a post-mount `useEffect` to avoid hydration mismatch) against a UTC hours table and renders "Open now · closes … UTC" / "Closed · opens [Day] … UTC". The demo intentionally pins the store to **UTC** (labeled in the badge + the Hours card) rather than a real timezone — a real Frontdoor site would store the business's tz.
- **2026-06-30 — Waitlist wired: Route Handler + Upstash Redis + BotID** (supersedes the 2026-06-28 `NEXT_PUBLIC_WAITLIST_ENDPOINT` placeholder, now removed). `app/api/waitlist/route.ts` (POST) runs an invisible **Vercel BotID** check (`checkBotId`), drops a hidden **honeypot** (`company`) field, validates the email server-side, then `zadd`s it to an Upstash Redis sorted set `waitlist` (dedupes, ordered by signup time, viewable in the Upstash console). `EmailCapture.tsx` posts same-origin to `/api/waitlist`. BotID config = `withBotId()` in `next.config.ts` + `initBotId()` in `instrumentation-client.ts`. **Provisioning (owner, in Vercel):** add Upstash for Redis (Storage tab or `vercel install upstash`) → Vercel injects store-prefixed vars (e.g. `frontdoorapp_KV_REST_API_URL`/`_TOKEN`; the route reads those with an `UPSTASH_REDIS_REST_*` fallback — all server-only, **not** `NEXT_PUBLIC_`). This is the app's first server endpoint; its surface is covered by BotID + honeypot + server-side validation (optional Upstash rate-limit can be added).
- **2026-06-30 — Dark mode + motion + richer demo.** **Dark mode** is class-based: `globals.css` flips the single-purpose color *variables* under `.dark` so most components adapt with zero edits; `bg-white` and brand text get explicit `.dark` rules; the navbar logo uses `dark:brightness-0 dark:invert`. The initial theme is applied before paint by a no-flash inline script in `layout.tsx` — this is the **one intentional `dangerouslySetInnerHTML`** in the app (a static string, no user data; do **not** treat it as a regression of the "no XSS sinks" posture). `ThemeToggle` flips + persists it; **defaults to light** (dark only when the user toggles it — changed 2026-06-30 from system-default). `/demo` is pinned to light via `.force-light` (it's a customer's own site — its root must NOT carry `bg-white`, or `.dark .bg-white` would win on specificity). **Effects:** smooth theme transition, button `active:scale`, and CSS scroll-driven `.reveal` on sections (all reduced-motion safe). **Demo** gained auto-scrolling gallery + reviews **marquees** (pause on hover) and a **click-for-details menu modal** (`components/demo/Menu.tsx`, client).
- **2026-06-30 — `/demo` rebuilt as a real bakery website + local photos.** Now a full site (photo-cover hero, nav, photo menu, gallery, reservations, reviews, footer) instead of a single card-stack page; section spacing tightened site-wide; menu prices show 2 decimals. Photos live in `public/demo/` — royalty-free, keyword-fetched from LoremFlickr and **manually vetted** (its tags return junk surprisingly often: corn dogs for "croissant", a cat statue for "wholemeal", laptop/café lifestyle shots — re-pick visually if regenerating). Served via plain `<img>`.
- **2026-06-30 — Added a `/demo` live-example page + transparent logo.** `/demo` is a clickable Bella's Bakery mock (website, menu, book-a-table, reviews) linked from the hero PresenceCard rows — it shows prospects "what you get" (`noindex`). The navbar logo (`public/logo-lockup.png`) is now a **transparent** PNG so it matches the translucent `bg-white/80` navbar: chroma-keyed (near-white → alpha 0) and auto-cropped from `assets/logo_banner.jpg` via a one-off `sharp` script (sharp is installed; the script wasn't kept — re-derive if the logo changes). Its aspect ratio is 1543×322 — keep the `next/image` `width`/`height` in `Header.tsx` and `PresenceCard.tsx` in sync if regenerated. Also: How-it-works step labels read "Step 1/2/3".
- **2026-06-30 — Brand is `frontdoorapp.co` everywhere** (not "Frontdoor"), and the real logo asset is used wherever the logo appears (supersedes the footer-wordmark detail in the logo entry below). All user-facing copy, the footer wordmark (`Wordmark`), SEO metadata, and the PresenceCard "powered by" now read **frontdoorapp.co** (lowercase, ".co" in brand blue) and use `public/logo-lockup.png`. The hand-built `DoorMark` is no longer used as the logo anywhere user-facing — only the Mission decorative accent and the favicon still use it (a known consistency gap; see **Known gaps**). Copy conventions locked in: "businesses" (not "small business"), "social media" (not "social").
- **2026-06-30 — Repositioned to a managed, done-for-you service** (was the generic "one home for your presence" framing). Per the product one-pager, Frontdoor *sets up and runs* a business's whole digital presence on autopilot. Landing page rewritten: new hero ("You do what you do best. We make the world see it."), table-stakes problem framing, a **How it works** section, and the **8 actual services** (Landing Page, Social Media Presence, Business Email, Analytics, Content Generation, Social Media Automation, Natural-language Automations, Platform Support). The source one-pager is marked *Confidential*, so its internal strategy (phased roadmap, "SaaS economics"/moat, resold vendor names) was **deliberately excluded** from the public site, and the "OpenClaw" codename was softened to "natural-language automations".
- **2026-06-30 — Header/footer logo now uses the real brand asset** — reverses the 2026-06-28 vector-logo decision *for the header*. The hand-built `DoorMark` SVG was inconsistent with the asset, so the header uses a cropped `public/logo-lockup.png` (door + "frontdoorapp.co" lockup) and the footer uses a "Frontdoor" text wordmark. `DoorMark` stays for small decorative marks and the favicon.
- **2026-06-28 — Shipped: live at [frontdoorapp.co](https://frontdoorapp.co).** Deployed to Vercel (zero-config; auto-deploys on push to `main`). ⚠️ Note: this went public **before** the open security-headers and waitlist-backend items were done — they are now *live gaps*, not pre-launch TODOs (see **Known gaps**).
- **2026-06-28 — Stack: Next.js + Tailwind v4** (over plain static HTML or Vite/React). The repo is meant to grow into the real web client, not stay a one-off marketing page. Next.js is the canonical zero-config Vercel target, leaves room for a future dashboard/auth/API routes, and has the strongest SEO/metadata story. Tailwind v4's CSS-first `@theme` keeps brand tokens in one place (`globals.css`).
- **2026-06-28 — Email capture is a documented placeholder.** `EmailCapture.tsx` optimistically shows success and only POSTs if `NEXT_PUBLIC_WAITLIST_ENDPOINT` is set. Why: ship a usable page without committing to a backend. **Before launch:** point that env var at a real endpoint (Route Handler / Formspree / Resend); the endpoint must do server-side validation, rate-limiting, and abuse protection — the client does none. A real secret (e.g. an API key) must **not** use the `NEXT_PUBLIC_` prefix, which would ship it in the browser bundle.
- **2026-06-28 — Logo is rendered as vector, not the raster file.** Header/footer use the hand-built `DoorMark` SVG + a styled wordmark for crispness and no baked-in whitespace; the raster JPGs are reserved for social/OG previews and the apple icon.
- **2026-06-28 — Security reviewed: clean for a static deploy.** No backend → minimal attack surface; no XSS sinks (no `dangerouslySetInnerHTML`), no secrets in code, external links carry `rel="noopener noreferrer"`. Open defense-in-depth follow-ups (not blockers): no security response headers yet — add CSP / `X-Frame-Options` / HSTS via `headers()` in `next.config.ts` before a public launch. The `npm audit` "2 moderate" is a transitive `postcss` inside Next's **build** tooling, not exploitable in a static build — **do not** "fix" it by downgrading Next.
- **2026-06-28 — `.claude/settings.local.json` is gitignored in-repo** (it's per-developer). The shareable counterpart, `.claude/settings.json`, may be committed.

## Known gaps / TODO posture

**Now live — these gaps are in production** (the site launched before they landed):
- No security response headers (CSP / `X-Frame-Options` / HSTS) — add via `headers()` in `next.config.ts`.

Other:
- **Design consistency:** the `app/icon.svg` favicon and the Mission band's decorative door still use the hand-built `DoorMark`, which doesn't match the real logo asset (`public/logo-lockup.png`) used everywhere else — regenerate the favicon from the asset and decide on the Mission accent.
- Waitlist signups don't persist until the owner runs `vercel install upstash` (the code is wired — see Decision Log).
- **Tests exist now** (Vitest, `tests/`) but cover behavior/logic + section smoke tests, not full-page rendering or visual regression; still **no linter** (ESLint not configured).
- License is **TBD** (per README) — don't assume an OSS license.

## Maintaining this file

- This is a memory store optimized for humans *and* LLMs. Prefer short, declarative statements and concrete reasoning over prose.
- When you make a non-obvious or reversible decision, add a dated **Decision Log** entry with the *why* and any rejected alternative. Use absolute dates.
- Update **Posture** and **Known gaps** when reality changes (backend lands, tests added, headers shipped, license chosen).
- Keep product/brand narrative in README.md and engineering posture/rationale here. Don't duplicate between them.
